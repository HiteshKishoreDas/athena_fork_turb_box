<comment>
problem   = Turbulence with power-law PS
reference =
configure = --prob=mix_layer

<job>
problem_id = Turb    # problem ID: basename of output filenames

<output1>
file_type  = hst        # History data dump
dt         = 0.0005       # time increment between outputs

<output2>
file_type  = tab        # Binary data dump
variable   = prim       # variables to be output
dt         = 0.001        # time increment between outputs

 
<output4>
file_type   = tab
dt          = 0.001
variable    = user_out_var

<time>
cfl_number = 0.3        # The Courant, Friedrichs, & Lewy (CFL) Number
nlim       = -1         # cycle limit
tlim       = 0.03         # time limit
integrator  = rk2       # time integration algorithm
xorder      = 2         # order of spatial reconstruction
ncycle_out  = 1         # interval for stdout summary info

<mesh>
nx1        = 512        # Number of zones in X1-direction
x1min      = 0.0       # minimum value of X1
x1max      = 0.1        # maximum value of X1
ix1_bc     = periodic   # inner-X1 boundary flag
ox1_bc     = periodic   # outer-X1 boundary flag

nx2        = 1        # Number of zones in X2-direction
x2min      = 0.0       # minimum value of X2
x2max      = 0.1        # maximum value of X2
ix2_bc     = periodic   # inner-X2 boundary flag
ox2_bc     = periodic   # outer-X2 boundary flag

nx3        = 1        # Number of zones in X3-direction
x3min      = -0.1      # minimum value of X3
x3max      = 0.2       # maximum value of X3
ix3_bc     = reflecting # inner-X3 boundary flag
ox3_bc     = reflecting # outer-X3 boundary flag

refinement = none

<meshblock>
nx1        = 512
nx2        = 1 
nx3        = 1 

<hydro>
gamma     = 1.666666666667 # gamma = C_p/C_v
pfloor    = 8e-08

# User-defined variables:
<problem>

cloud_flag   = 0

cooling_flag = 1                # 0 for no cooling and >0 for cooling

amb_rho      = 0.00016      # Density of the ambient medium

T_floor      = 10000    # Floor temperature
T_ceil       = 100000000     # Ceiling temperature
T_hot        = 16000000      # Hot medium temperature
T_cold       = 80000     # Cold medium temperature
T_cut_mul    = 2.0  # T_cut = T_hot * T_cut_mul

Xsol         = 1.0     # H-abundance in solar abundance
Zsol         = 1.0     # Metallicity in solar abundance 

Lambda_fac   = 2.0  # Multiplier for boosting cooling

B_x          = 0.0     # Magnetic field in x-direction
B_y          = 0.0     # Magnetic field in y-direction
B_z          = 0.0     # Magnetic field in z-direction
