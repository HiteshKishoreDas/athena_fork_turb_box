<comment>
problem   = Turbulence with power-law PS
reference =
configure = --prob=mix_layer

<job>
problem_id = Turb    # problem ID: basename of output filenames

<output1>
file_type  = hst        # History data dump
dt         = !!HST_DT!!      # time increment between outputs

<output2>
file_type  = hdf5        # Binary data dump
variable   = prim       # variables to be output
dt         = !!OUT_DT!!      # time increment between outputs

<output3>
file_type  = rst       
dt         = !!RST_DT!!      
 
<output4>
file_type   = hdf5
dt          = !!USER_OUT_DT!!
variable    = user_out_var

<time>
cfl_number = 0.3        # The Courant, Friedrichs, & Lewy (CFL) Number
nlim       = -1         # cycle limit
tlim       = !!TLIM!!   # time limit
integrator  = rk2       # time integration algorithm
xorder      = 2         # order of spatial reconstruction
ncycle_out  = 1         # interval for stdout summary info

<mesh>
nx1        = !!NX1!!        # Number of zones in X1-direction
x1min      = !!X1MIN!!       # minimum value of X1
x1max      = !!X1MAX!!        # maximum value of X1
ix1_bc     = periodic   # inner-X1 boundary flag
ox1_bc     = periodic   # outer-X1 boundary flag

nx2        = !!NX2!!        # Number of zones in X2-direction
x2min      = !!X2MIN!!       # minimum value of X2
x2max      = !!X2MAX!!        # maximum value of X2
ix2_bc     = periodic   # inner-X2 boundary flag
ox2_bc     = periodic   # outer-X2 boundary flag

nx3        = !!NX3!!        # Number of zones in X3-direction
x3min      = !!X3MIN!!      # minimum value of X3
x3max      = !!X3MAX!!      # maximum value of X3
ix3_bc     = outflow    # inner-X3 boundary flag
ox3_bc     = outflow    # outer-X3 boundary flag

refinement = none

<meshblock>
nx1        = !!NX1_MESH!!  
nx2        = !!NX2_MESH!! 
nx3        = !!NX3_MESH!! 

<hydro>
gamma     = 1.666666666667 # gamma = C_p/C_v
pfloor    = !!P_FLOOR!!

# User-defined variables:
<problem>

DEBUG_FLAG = 0 

cloud_flag   = 0

cooling_flag = 1                # 0 for no cooling and >0 for cooling
shift_flag   = !!SHIFT_FLAG!!   # 0 for no adaptive shift and >0 for otherwise

amb_rho      = !!AMB_RHO!!      # Density of the ambient medium

front_thickness = !!FRONT_TH!!  # thickness of the interface
v_shear         = !!V_SHEAR!!   # Shear velocity

v_shift      = !!V_SHIFT!!    # Frame shift for the simulation in z

knx_KH       = !!KNX_KH!!
kny_KH       = !!KNY_KH!!
amp_KH       = !!AMP_KH!!     # Amplitude = amp_KH * v_shear

T_floor      = !!T_FLOOR!!    # Floor temperature
T_ceil       = !!T_CEIL!!     # Ceiling temperature
T_hot        = !!T_HOT!!      # Hot medium temperature
T_cold       = !!T_COLD!!     # Cold medium temperature
T_cut_mul    = !!T_CUT_MUL!!  # T_cut = T_hot * T_cut_mul

Xsol         = 1.0     # H-abundance in solar abundance
Zsol         = 1.0     # Metallicity in solar abundance 

Lambda_fac   = !!LAM_FAC!!  # Multiplier for boosting cooling

B_x          = !!B_X!!     # Magnetic field in x-direction
B_y          = !!B_Y!!     # Magnetic field in y-direction
B_z          = !!B_Z!!     # Magnetic field in z-direction
